package com.api.listProf;

import com.api.listProf.entity.Contatos;
import com.api.listProf.entity.Profissionais;
import com.api.listProf.service.ProfissionaisService;
import org.hibernate.annotations.CreationTimestamp;
import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class ListProfApplicationTests {

	@Test
	void contextLoads() {
/*	List<Profissionais> profList = new ArrayList<>();
		for (int i = 0; i < profList.size() ; i++) {
			Profissionais profl = new Profissionais();
			profl.setId_prof(1);
			profl.setNome("nomeTeste");
			profl.setCargo("desenvolvedor");
			profl.setNascimnento(Timestamp.valueOf("2000-01-10 00:00:00.0"));
			profl.setCreated_date(Timestamp.valueOf("2023-11-14T01:00:58.544+00:00"));
		profList.add(profl);
		}

		List<Contatos> contList = new ArrayList<>();
		for (int i = 0; i < contList.size() ; i++) {
			Contatos cont = new Contatos();
			cont.setId(1);
			cont.setNome("nomeTeste");
			cont.setContato("064992642340");
			cont.setCreated_date(Timestamp.valueOf("2023-11-14T01:00:58.544+00:00"));
			contList.add(cont);
		}
		System.out.println(contList);
      System.out.println(profList);
	*/
	}
}
